const express = require('express');
const publicController = require('../controllers/admin_controller');
// const app = express();
const router = express.Router();

// router.post("/register",publicController.register);

module.exports  = router;